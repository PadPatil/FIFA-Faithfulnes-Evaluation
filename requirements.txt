# Core pipeline dependencies. Tested with Python 3.11.
# Everything below is enough to run the full notebook end-to-end using the
# "mock" captioner/VQA backends (no GPU, no downloaded model weights required).
numpy>=1.26,<3
opencv-python-headless>=4.9
networkx>=3.2
pandas>=2.2
matplotlib>=3.8
scikit-learn>=1.4
scipy>=1.12
spacy>=3.7
tqdm>=4.66
pillow>=10.2
ipykernel>=6.29
nbclient>=0.10
nbformat>=5.10
jupyter>=1.0

# After installing, download the spaCy English model:
#   python -m spacy download en_core_web_sm
